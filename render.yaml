services:
  - type: web
    name: mindset-bots
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main_cloud.py
    healthCheckPath: /health
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: BOT_TOKEN_2
        sync: false
